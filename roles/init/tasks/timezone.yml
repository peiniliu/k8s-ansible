# Set the time zone Shanghai
  - name: Set the time zone Spain
    shell: timedatectl set-timezone Europe/Madrid
#    shell: timedatectl set-timezone Asia/Shanghai
# Start chronyd service,Set clock synchronization
#  - name: Set clock synchronization
#    service:
#      name: chronyd
#      state: started
#      enabled: yes
